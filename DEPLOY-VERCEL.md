# توحيد الموقع بين Lovable و Vercel

هذا الملف مرجع جاهز: انسخ منه القيم إلى Vercel، ونفّذ الخطوات بالترتيب.

## 1) المفاتيح المطلوبة في Vercel

اذهب في Vercel إلى: Project → Settings → Environment Variables، وأضف التالي لكل من
Production و Preview و Development.

| الاسم | القيمة |
| --- | --- |
| `VITE_SUPABASE_URL` | Project URL من Supabase |
| `VITE_SUPABASE_PUBLISHABLE_KEY` | Publishable key |
| `VITE_SUPABASE_PROJECT_ID` | معرّف المشروع (الجزء الأول من رابط Project URL) |
| `SUPABASE_URL` | نفس Project URL |
| `SUPABASE_PUBLISHABLE_KEY` | نفس Publishable key |
| `DB_SERVICE_KEY` | Service role key (سري تمامًا) |
| `GATE_SESSION_SECRET` | قيمة عشوائية 64 حرفًا (نفسها في Lovable) |
| `GATE_HASH_SALT` | قيمة عشوائية 64 حرفًا (نفسها في Lovable) |

ملاحظة: `Direct connection string` و`CLI setup commands` غير مطلوبين في Vercel،
يُستخدم الأول عند نقل البيانات فقط.

## 2) من أين تجلب كل مفتاح

- Project URL و Publishable key و Service role key: من إعدادات مشروعك في
  Supabase، قسم **API Keys**. مفتاح الـ service role هو المفتاح الإداري ولا يوضع
  أبدًا في متغير يبدأ بـ `VITE_`.
- `GATE_SESSION_SECRET` و`GATE_HASH_SALT`: تصنعهما بنفسك مرة واحدة. أي مولّد
  كلمات مرور طويل يكفي، أو من الطرفية:

  ```bash
  openssl rand -hex 32
  ```

  نفّذ الأمر مرتين لتحصل على قيمتين مختلفتين، احفظهما عندك، ثم ضع نفس القيمتين
  في Vercel وفي Lovable.

## 3) تنبيه مهم عن `GATE_HASH_SALT`

رمز الدخول والصورة السرية مخزّنان في قاعدة البيانات مشفَّرَين بهذا الملح.
تغييره يجعل الرمز والصورة الحاليين غير صالحين.

لذلك بعد تثبيت القيمة الجديدة في المكانين، نحذف سجل الاعتماد الحالي مرة واحدة،
ثم أول رمز وأول صورة تُدخلهما في صفحة التحقق يصبحان الرسميين للنظام في
النسختين معًا.

## 4) الترتيب الصحيح للتنفيذ

1. تصنع القيمتين العشوائيتين وتحفظهما.
2. تضع القيمتين + مفاتيح Supabase في Vercel.
3. تسلّمني نفس القيمتين + مفاتيح Supabase عبر النموذج الآمن في Lovable (لا تكتبها في المحادثة).
4. أحذف سجل الاعتماد القديم للبوابة.
5. أنقل الهيكلة والمحتوى والصور إن كنا نغيّر قاعدة البيانات.
6. تعمل Redeploy في Vercel (المتغيرات لا تُطبَّق إلا بنشر جديد).
7. نجرّب: الرئيسية، الشخصيات، الأحداث، الستريمرز، ثم التحقق ولوحة التحكم.

## 5) إن ظهرت مشكلة في بناء Vercel

المشروع يُبنى افتراضيًا لهدف Cloudflare. إذا فشل النشر على Vercel أو ظهرت
صفحة خطأ من الخادم، أضف متغيرًا واحدًا:

```text
NITRO_PRESET = vercel
```

ثم أعد النشر.

## 6) الوضع الحالي للبيانات

- 21 قسم محتوى (11 شخصية + 10 أحداث)
- 58 صورة
- إعدادات الموقع: سجل واحد
- اقتراح واحد
- سجل اعتماد واحد للبوابة (سيُعاد ضبطه في الخطوة 4)
